import React from "react";
import Layout from "../templates/default";
import styled from "react-emotion";

const H2 = styled.h2`
  text-align: center;
`

export default () => (
  <Layout>
    <H2>About Page</H2>
  </Layout> 
);
